// TODO (ส่วนที่ 6): POST /api/hint
// - Zod validate, rate limit 10 req/min/user
// - จำกัด 3 hint/challenge, ระดับ hint เพิ่มขึ้นตามจำนวนครั้ง
// - เรียก Claude API ตอบเป็นภาษาไทย ไม่บอก flag
export {};
